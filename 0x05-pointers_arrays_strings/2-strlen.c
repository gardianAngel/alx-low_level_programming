#include "main.h"
/**
 * _strlen -> a function that  returns the lenght of a string.
 * @s: points for the string to be chacked
 * REtur: void
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
		return (i);
}
