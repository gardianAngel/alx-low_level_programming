#include "main.h"

/**
 * add -> adds two integer and returns the result
 * @i: parameter
 * @j: parameter
 *
 * Return: returns the result
 **/
int add(int i, int j)
{
int x  = j + i;
	return (x);
}
